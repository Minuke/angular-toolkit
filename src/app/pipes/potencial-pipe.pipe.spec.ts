import { PotencialPipePipe } from './potencial-pipe.pipe';

describe('PotencialPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PotencialPipePipe();
    expect(pipe).toBeTruthy();
  });
});
